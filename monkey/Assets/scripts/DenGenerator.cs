using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class DenGenerator
{
    public int denWidth;
    public int denLength;

    // Start is called before the first frame update
    public DenGenerator(int denWidth, int denLength)
    {
        this.denWidth = denWidth;
        this.denLength = denLength;
    }

    // Update is called once per frame
    internal object CalculateRooms(int maxIteration, int roomWidthMin, int roomLengthMin)
    {
        
    }
}
